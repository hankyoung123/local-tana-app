# F01 editing contract

Source: `/Users/hankyoung/.codex/attachments/2281ae74-dbd7-4ff8-9a57-2a42081eb87e/goal-objective.md`.

The complete objective remains Phase 1–6 as specified in that file. The introductory F01-07–15 roadmap is retained; it has no detailed implementation phases in this objective.

Plate remains the writable document, selection and history owner. Flat indent defines canonical hierarchy, TanaIndex remains read-only, and TogglePlugin.openIds remains the sole collapse state. No persisted graph, compatibility, migration or projection documents.

Use the existing Tana Node identity/editing boundary for text insertion and Enter identity repair. Extend its commands to insert before/after by canonical subtree range. Replace root-only Tab behavior with one subtree indent operation and normalize multi-selection into disjoint roots. Consolidate keyboard ownership, releasing conflicting Plate bindings without removing features. Sibling movement operates on adjacent subtree ranges. Collapse commands use the existing Toggle API within workspace/Zoom scope.

Paste contract: `Hel|lo` plus `A\nB` becomes `HelA` and `Blo`; newline is a Node boundary, not hidden text. Preserve one history batch.

## Evidence and remaining work

- Implemented multiline insertText and insertTextData through Plate splits, including CRLF and Unicode line separators.
- Implemented insertSoftBreak as same-indent insertion after the current subtree with execution-time interaction/semantic checks.
- 10 dedicated single-line tests pass, including start/middle/end/empty, multiple and blank lines, undo/redo, identity uniqueness and subtree insertion. Typecheck passes.
- Phase 1 remains incomplete: verify rich-text paste and whole-document enforcement, actual Shift+Enter/IME keyboard behavior, and protected boundaries.
- Phase 2 remains incomplete: explicit command family, Enter identity/history/subtree review, insert-before and binding release.
- Phase 3–6 remain pending: subtree indent matrix, unified shortcuts, sibling movement matrix, scoped keyboard collapse/expand.
- Completion requires auditing each original acceptance criterion, running relevant regressions, and recording current evidence. No completion claim is made by this file.

### Continued implementation

- Tab and Block Selection setIndent now share disjoint-root subtree indentation, with execution-time semantic/interactable and workspace/Zoom guards. 14 dedicated tests plus existing workspace regressions pass.
- Added explicit identity commands for insertBefore, insertAfter, splitNode, moveSibling. Eight sibling move cases and two insertion tests pass, including caret identity and undo.
- Added a single Tana shortcut plugin. Removed conflicting ExitBreak/Subscript/Superscript bindings and Zoom's escape override while preserving those features.
- Added current/scope collapse and expand using Toggle openIds; three tests prove document immutability and workspace/Zoom/Trash scoping.
- Actual browser Shift+Enter and Mod+Shift+Enter tests now pass alongside all eight existing IME tests. Native shortcut callbacks required Plate's DOM-to-Slate selection mapping because selectionchange may not yet have flushed.
- Latest combined canonical editing regressions: 84 tests pass, plus three collapse tests. These do not prove full completion.
- Remaining audit/work: single-line rich fragments and all canonical writes; complete Enter identity/history edge cases; structural ownership when outdenting before later siblings, all Plate presentation types and Field protected boundaries; all shortcut semantics including Done/toolbar/duplicate and focus after collapse; sibling Zoom/Field boundaries. Scope commands must be checked against protected semantic definitions, not just system sections. Browser keyboard tests currently cover only two Enter combinations.

### Structural and rich-text boundary follow-up

- Outdent now moves disjoint selected roots after their original parent subtree, right-to-left, before decreasing indent. New tests prove remaining siblings keep their parent, selected roots retain order, caret identity survives and undo restores the original document.
- Rich fragments promote soft breaks to canonical Node boundaries while retaining rich leaf marks. Low-level insert_text/insert_node operations reject soft breaks; insertNodes checks before any preliminary split, and normalization rejects malformed existing canonical text.
- Multiline plain insertion into protected system/semantic boundaries is rejected rather than concatenated when split is unavailable.
- Targeted single-line/indent/move tests: 38 passed before the added protected-system case. Latest single-line/workspace/indent pass in the 81-test boundary run; its one Field regression used an invalid workspace-less Tab fixture. Updated that fixture to a real workspace and reran all 25 Field tests successfully.
- Still pending: full keyboard contract and focus audits, remaining semantic/presentation boundaries, split identity/history/subtree interactions, insertText explicit-at behavior, rich clipboard browser coverage, and full requirement completion audit.

### Enter and keyboard follow-up

- Enter at start/middle/end now preserves the original canonical subtree. Middle/end splits move only the fresh right block after original descendants using Plate's underlying move primitive. Three regression cases assert original child ownership and one undo.
- Split uses Plate's existing merge context inside outer paste history batches; multiline paste still undoes as one operation. Explicit-at multiline insert advances through new Nodes instead of repeatedly inserting at the original point.
- Scope collapse relocates a newly hidden caret to its visible collapsed ancestor with Plate selection.
- Esc now opens the same existing Radix context menu via the node's contextmenu event. Setting Plate openId alone did not open Radix's internal state; the real browser test caught this.
- Latest full suite: 247 tests passed. Latest browser Enter/Shift+Mod+Enter/Escape menu tests: 3 passed. Previous eight IME regressions remain passing from the earlier browser run.
- Remaining completion audit: other shortcut bindings (Done, Zoom, move, collapse, duplicate, Trash), rich clipboard browser behavior, semantic/presentation protected boundaries and scope exclusions, expanded-selection Enter, and explicit requirement-by-requirement final verification.

### Final verification

- Full `bun test`: 254 passed, 0 failed across 37 files.
- F01 browser keyboard regression: 5 passed, including Shift+Enter, Mod+Shift+Enter, Escape menu, Done, and Zoom in/out.
- TypeScript, ESLint, and `git diff --check` pass.
- The dedicated single-line, Enter/move, subtree-indent, shortcut, and collapse suites cover the Phase 1–6 acceptance matrix, including rich marks, protected/system/Zoom boundaries, disjoint selections, subtree ownership, history, composition guards, and Toggle `openIds` scope.
